rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read/write to 'uploads' folder for form attachments
    match /uploads/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
